# SkyEar

Turns a security camera you already own into an acoustic sensor for aircraft,
and eventually drones. Every detection is checked against live ADS-B, so the
system marks its own homework.

Install, open SkyEar in the sidebar, and fill in one form. Your camera's
address and password never leave this machine.
